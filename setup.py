from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Integrated DVC pipeline with the entire Project pipeline',
    author='Shrijeet Kumar',
    license='',
)
